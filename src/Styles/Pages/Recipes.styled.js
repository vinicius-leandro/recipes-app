import styled from 'styled-components';

const RecipesCardContainer = styled.section`
    margin-bottom: 3rem;
    width: 100vw;
    display: flex;
    flex-wrap: wrap;

    a {
    text-decoration: none;
    }
`;

export default RecipesCardContainer;
